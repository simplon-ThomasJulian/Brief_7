# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

schedules:
- cron: "0 * * * *"
  displayName: Daily midnight build
  branches:
    include:
    - main

pool:
  vmImage: ubuntu-latest

steps:
- task: Kubernetes@1
  inputs:
    connectionType: 'Kubernetes Service Connection'
    kubernetesServiceEndpoint: 'kubernetes'
    command: 'get'
    arguments: 'deployments'
    outputFormat: 'json'
  name: "kube"

- task: CmdLine@2
  inputs:
    script: |
      versionnew=$((curl 'https://hub.docker.com/v2/repositories/simplonasa/azure_voting_app/tags' | jq '."results"[0]["name"]')| sed 's/^.//;s/.$//')
      echo $versionnew
      echo "##vso[task.setvariable variable=vernew;]$versionnew"
      cd /home/vsts
      git clone https://github.com/simplon-ThomasJulian/Brief_7.git
      cd Brief_7
      ls
- task: Kubernetes@1
  inputs:
    connectionType: 'Kubernetes Service Connection'
    kubernetesServiceEndpoint: 'kubernetes'
    command: 'apply'
    useConfigurationFile: true
    configuration: '/home/vsts/Brief_7/kubernetes/'
    arguments: ''
    secretType: 'generic'
    outputFormat: 'json'

- task: CmdLine@2
  inputs:
    script: |
      echo $(vernew)
      echo $KUBE_KUBECTLOUTPUT | jq '.items[].spec.template.spec.containers[].image' |cut -d: -f2|sed 's/"//'